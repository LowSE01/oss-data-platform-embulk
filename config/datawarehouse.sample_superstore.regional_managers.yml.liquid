# @formatter:off
in:
{% include 'include/connection/datalake' %}
  query: |
    select        regional_managers.regional_manager_id,
                  regional_managers.regional_manager,
                  regional_managers.region_id
    from          sample_superstore_regional_managers           regional_managers

out:
{% include 'include/connection/datawarehouse' %}
  mode: replace
  table: sample_superstore_regional_managers
  column_options:
    regional_manager_id: {"bigint not null"}
    regional_manager: {"varchar(32)"}
    region_id: {"bigint"}
